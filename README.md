# CadArena

## Overview
CadArena is a FastAPI application for CAD operations.

## Quick Start
Create local env file first:

```bash
cp backend/.env.example backend/.env
```

To run the development server:

```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```
